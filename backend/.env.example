# Backend Environment Variables - PRODUCTION TEMPLATE
# Copy this file to .env and fill in your actual values

# Environment
NODE_ENV=production
PORT=3000

# Database - MongoDB Atlas
DB_USER=your_db_user
DB_PASSWORD=your_db_password
DB_URI=mongodb+srv://your_db_user:your_db_password@cluster0.xxxxx.mongodb.net/promodoro?retryWrites=true&w=majority&appName=Cluster0

# Security - Generate a strong JWT secret
# Use: node -e "console.log(require('crypto').randomBytes(64).toString('hex'))"
JWT_SECRET=your_strong_jwt_secret_here_minimum_32_characters

# CORS Origins (comma-separated for multiple origins)
CORS_ORIGIN=https://promodoroapp.vercel.app
